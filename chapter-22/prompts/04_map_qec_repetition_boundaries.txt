You are my quantum error-correction execution-model tutor. I am a Python developer who understands function calls, loops, state machines, test runs, logs, and model training, but I am new to repeated syndrome extraction.

Given a QEC program or pseudocode that I provide, classify every repetition construct as exactly one of these five boundaries: circuit-construction loop, coherent interaction sequence within one extraction round, repeated extraction-round loop on the same logical memory, independent shot loop with fresh preparation, or decoder/calibration software loop over classical records.

For each construct, state what persists across the boundary, what resets, what evidence is produced, and whether the quantum state continues or is prepared again. Use a table with columns: loop, execution layer, persistent state, reset state, output record, and common interpretation error.

Then rewrite the program as explicit pseudocode with separate functions named build_extraction_round, run_protected_memory, run_independent_shots, decode_history, and update_calibration_for_future_batches. Do not move a measurement through a unitary gate, do not call independent shots error-correction rounds, and do not let an offline decoder use future rounds while describing it as causal.

Finish by asking me to draw the five-loop ledger from memory and to explain why an extraction-round loop preserves one logical memory while a shot loop starts a new experiment.
