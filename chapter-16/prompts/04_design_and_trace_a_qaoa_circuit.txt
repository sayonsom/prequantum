I am a software developer designing a small QAOA experiment. I will provide the original decision variables, objective, constraints, QUBO convention, bit order, classical baselines, success metric, and available execution target. Help me design backward from the classical evidence instead of beginning with an arbitrary circuit.

Return these records in order:
1. An evidence contract naming the decoded output, feasibility check, objective metric, uncertainty, baseline, resource boundary, and strongest conclusion the experiment could support.
2. A formulation ledger that derives the QUBO and Ising coefficients, preserves the constant offset, states the binary-to-spin map, and verifies tractable assignments against the original objective and constraints.
3. A logical QAOA circuit for the smallest useful depth. State the variable-to-qubit map and displayed bit order. For every preparation, cost, mixer, and measurement operation, name its operands and angle. Derive every RZ, RZZ, or other Pauli-rotation angle from the Hamiltonian convention.
4. A left-to-right wire trace for one basis state and a separate explanation of how the cost layer changes phases before the mixer changes the measurement distribution. Do not describe the cost layer as directly lowering a bit string's classical cost.
5. A repetition ledger that distinguishes host-language coefficient construction, layer construction, the coherent sequence of p layers, parameter evaluations, transpilation, independent shots, final sampling, restarts, depth comparisons, and benchmark-instance loops. For each mechanism, state what repeats, where its state lives, and what stops it.
6. A target-compilation record containing the basis gates, coupling map, layout, routing, optimization level, seed, logical and compiled depth, two-qubit operation count, final bit mapping, and an ideal logical-versus-compiled equivalence check.
7. Minimal Qiskit code plus tests for coefficient equivalence, bit order, one hand-computable Hamiltonian term, normalization, logical probabilities, compiled ideal equivalence, decoded feasibility, and agreement with an exact classical baseline.
8. A symbolic evaluation-count expression in terms of depth, parameter evaluations, measurement groups, shots, restarts, and benchmark instances before substituting numbers.

Keep the classical parameter search, stopping rule, decoding, constraint validation, and baseline comparison outside one coherent circuit execution. If a constraint-preserving mixer is proposed, state the feasible subspace, prove or test the conserved quantity, and verify that the initial state is feasible. Treat transpilation as a target-specific classical transformation rather than as another QAOA layer. Mark unknown target features and costs instead of inventing them. Do not infer quantum advantage from Hilbert-space size, a favorable toy result, or state-vector simulation.
